services:
  # Postgres SQL database exposed on port 15432
  # Database: postgres
  # Username: postgres
  # Password: example
  postgres:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - 15432:5432
